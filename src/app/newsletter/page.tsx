"use client";
import StudioBoard from "@/app/studio/page";

export default function NewsletterPage() {
  return (
    <StudioBoard
      lockedChannel="Newsletter"
      pageTitle="Newsletter Content Manager"
      pageSubtitle="Generate, review and schedule email newsletters"
    />
  );
}
