"use client";

/** /seo — PERFORMANCE, the landing sub-app of the cockpit. */

import SearchPerformance from "./SearchPerformance";

export default function SeoPerformancePage() {
  return <SearchPerformance />;
}
